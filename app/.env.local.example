# Solana RPC endpoint — server-side (hidden from browser bundle)
RPC_URL=https://api.devnet.solana.com

# Solana RPC endpoint — client-side (exposed for wallet adapter + tx signing)
# In production, use a public/rate-limited endpoint here
NEXT_PUBLIC_RPC_URL=https://api.devnet.solana.com

# FOMolt3D program ID (set after WS1 deployment)
PROGRAM_ID=11111111111111111111111111111111

# Base URL for this app (used for Blinks/Actions URLs)
NEXT_PUBLIC_BASE_URL=http://localhost:3000
